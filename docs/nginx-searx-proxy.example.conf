# مثال: بروكسي SearXNG على نفس نطاق موقع الصندوق (يحل CORS)
# ضع المسار داخل server { ... } لديك وعدّل upstream.

# upstream searx_backend {
#     server 127.0.0.1:18080;
# }

# location /api/searx/ {
#     proxy_pass http://searx_backend/;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }

# في iif-config.js ضع: searxngBase: 'https://iif.example.com/api/searx'
