node_modules/
# بيئات بايثون محلية (مثلاً translator)
.venv*/
venv/
__pycache__/
.DS_Store
*.log
.env
.env.local
.netlify/

# مخرجات تجهيز GitHub Pages (يُولَّد محلياً وفي CI)
gh-pages-dist/

# كاش SearXNG محلياً (يُبقى المجلد فقط)
engines/searxng/data/*
!engines/searxng/data/.gitkeep
# نسخة مرجعية من الإعداد الافتراضي (لتوليد جدول المحركات)
!engines/searxng/data/upstream-settings-default.yml

# محلي / أدوات — لا تُرفع
.minimax/
test-results/
playwright-report/

# سجلات/مخرجات عرضية من الطرفية أو أدوات (لا تُرفع)
cmdout.txt
final-step*.txt
netlify-deploy-log*.txt
netlify-help.txt
netlify-step*.txt
_git_run_log.txt

# ملفات تجريبية من جلسات Netlify/CLI (لا تُرفع)
argv.js
run-build.cjs
run-npx.cjs
run-user.cmd
user-run.ps1
netlify-api-list.txt

# ملفات قفل مؤقتة من Microsoft Office (~$filename.docx)
~$*

# مخرجات محلية كبيرة (عروض PPTX جاهزة) — السكربتات تحت scripts/ppt/
exports/

# نتيجة فك/اختبار QR مؤقتة
qr_decode_result.txt
