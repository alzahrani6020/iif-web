# نسخ قديمة من الصفحة الرئيسية (أرشيف)

هذه الملفات كانت نسخًا تجريبية/بديلة لـ `index.html` في المجلد الأب (`iif-fund-demo/`).

**الصفحة المعتمدة للتطوير والنشر:** `../index.html` (مسار واحد فقط من جذر `iif-fund-demo`).

لا تُحدَّث هذه النسخ تلقائيًا؛ احتفظ بها للمرجعية أو احذفها إذا لم تعد مطلوبة.
